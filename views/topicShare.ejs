<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title><%=title%></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0"/>
    <meta name="format-detection" content="telephone=no">
    <meta name="apple-mobile-web-app-capable" content="yes"/>
    <meta name="apple-mobile-web-app-status-bar-style" content="black"/>
    <link rel="shortcut icon" type="image/x-icon" src="/images/hlyd_logo.png"/>
    <link rel="stylesheet" href="/stylesheets/share.css">

</head>
<body class="">
<section class="">
    <div class="top-down-app">
        <a href="<%=appDownloadLink%>">
            <div class="ljyd-logo">
                <img src="/images/hlyd_logo.png" alt=""/>
            </div>
            <div class="app-info">
                <h4 class="name">
                    汇邻优店</h4>
                <div class="text">
                    邻里互动，同城交易
                </div>
            </div>
            <div class="button-down">
                免费下载
            </div>
        </a>
    </div>
    <header class="artical-title">
        <h1 class="headline"><%=title%></h1>
        <div class="artical-info">
            <span class="times">22分钟前</span>
            <a class="new-source"><%=author%></a>
        </div>
    </header>

    <article>
        <% if(content && content.length > 0) { %>
        <% content.forEach(( value ) => { %>
        <% if( value.type === 'COMP_TEXT') { %>
            <%var textArray = value.text.split("\n")%>
            <%textArray.forEach((value) =>{ %>
                <p><%=value%></p>
            <% }) %>
        <% } else if( value.type === 'COMP_IMG' ) { %>
        <div class="artical-image">
            <img src="<%=value.url%>">
        </div>
        <% }%>
        <% }) %>
        <% } %>
    </article>

    <div class="comment">
        <h3 style="text-align: center">精选留言</h3>

        <% if(comments && comments.length > 0) { %>
        <% comments.forEach(( value ) => { %>
            <% var avatar = value.avatar || "https://simors.github.io/ljyd_blog/ic_launcher.png"%>
            <div class="comment-item">
                <img src="<%=avatar%>" alt="">
                <div style=" margin-left: 10px">
                    <div style="color: #717171"><%=value.nickname%></div>
                    <p><%=value.content%></p>
                </div>
            </div>
        <% }) %>
        <% } %>
    </div>


    <div class="down-app">
        <a href="<%=appDownloadLink%>" class="button-down-app">下载汇邻优店，秀出美好生活</a>
    </div>
</section>

</body>

</html>
